{
  "name": "mangoro-hunter-backend",
  "version": "0.1.0",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@fastify/jwt": "^7.0.0",
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.0.0",
    "fastify": "^4.0.0",
    "luxon": "^3.5.0",
    "mammoth": "^1.11.0",
    "openai": "^4.0.0",
    "pdf-parse": "^2.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "prisma": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
